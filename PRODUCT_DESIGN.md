# 产品设计说明

## 定位

本项目面向显微图像识别课程设计、创新竞赛和科研演示，目标是把“上传样本 → 质量检查 → 模型识别 → 查看报告 → 人工反馈”的闭环做清楚。

## 首页设计

- 顶部导航：Logo、首页、开始识别、用户中心、退出
- 首页主体：左侧上传识别入口，右侧流程图和平台状态
- 上传框：支持拖拽和点击上传，限制格式和 12MB 大小
- 上传前检查：图像完整性、质量评估、结构化报告，使用可折叠说明
- 视觉风格：深蓝到浅蓝渐变背景，搭配青绿色和亮蓝按钮

## 历史识别记录

每条历史记录包含缩略图、日期、候选种类、置信度。点击记录后展开完整报告：

- 图片检测框标注
- 置信度热力图
- 候选分类条形图
- 结构化数据表
- PDF/CSV 导出
- 正确 / 存疑 / 错误反馈

## 教育科普

科普内容围绕鼓藻简介、典型种类图集、识别小技巧、生态监测价值和科研应用展开。页面使用真实样本风格图片，便于答辩时直观展示。

## 后端设计

后端使用 Flask 提供 REST API，SQLite 保存：

- 用户账号和登录 token
- 每次识别的完整 JSON 报告
- 用户人工反馈

模型接口采用“可用则调用 YOLO，不可用则走演示模型”的策略。后续只需替换 `backend/models/best.pt` 并安装 `ultralytics`，即可对接真实模型。

## 扩展方向

- 批量上传和批量报告导出
- 管理员复核台
- 数据集标注和训练入口
- PostgreSQL 部署版本
- 移动端小程序或 App 复用同一 REST API
