# 鼓藻鉴析前端设计规范

## 设计方向

本次重构采用 `DESIGN (2).md` 的 Stripe-inspired 设计系统，并转换为水生态科研语境：顶部使用柔和蓝绿渐变网格，主体以白色与浅水色画布承载内容，关键数据模块使用深水青控制台风格，按钮统一为胶囊形态。界面要专业、科技、精致、有层次，不做空泛营销页，首屏直接呈现可用的平台入口与数据产品感。

## 信息架构

- 顶部导航承载核心工作流：首页、图像检测、数据统计、分布图谱、物种档案、检测记录、管理后台。
- 首页由 Hero、实时识别预览、核心数据、功能介绍、技术亮点、平台优势和工作台摘要组成。
- 图像检测页分为模式切换、上传与样本信息、结果图、候选排序、置信度、形态解释和归档动作。
- 数据统计页面向管理者展示样本统计、检测趋势、类别分布、筛选表格和数据导出。
- 检测记录页面向登录用户展示账户统计、筛选、搜索、详情弹窗和报告预览。
- 管理后台面向模型训练与数据治理，包含训练资产、类别平衡、训练配置、日志、评估和模型版本。

## 视觉系统

- 主色：深水青 `#0B3B4A`、科研青绿 `#0F766E`、识别绿 `#16A085`。
- 强调色：水光青 `#2DD4BF`、显微蓝 `#38BDF8`、少量靛蓝 `#533AFD` 用于链接和重点图表。
- 背景：页面顶端使用蓝绿/青蓝/淡紫渐变网格；主体为 `#F6F9FC` 和白色画布。
- 深色模块：使用 `#102A43` / `#0B2536` 表示产品控制台、报告预览、训练日志和数据仪表区。
- 文本：主文本 `#0D253D`，次级文本 `#52657A`，弱文本 `#7B8EA3`。
- 边框：浅色发丝线 `#DDE7EF`；输入框使用更冷的 `#A8C3DE`。
- 圆角：按钮统一 999px 胶囊；输入 8px；卡片 12px；产品控制台 16px。
- 阴影：轻蓝色阴影为主，优先营造浮层感，避免厚重后台阴影。

## 排版与布局

- 字体使用 Inter / Segoe UI / Microsoft YaHei / system-ui；标题较轻但清晰，数字启用 tabular figures。
- 页面容器最大宽度 1360px，桌面端使用 2-3 栏网格，移动端收敛为单列。
- 首页 Hero 占据第一视口主体但保留后续内容露出；右侧必须有产品 UI 预览或数据控制台，不使用纯装饰图。
- 统计、表格和控制台区保持较高扫描密度；营销型大标题仅用于首页和页面引导区。

## 组件规范

- `primary-button`：主操作，胶囊形态，蓝绿渐变或深水青背景，至少 40px 高。
- `secondary-button`：白底描边胶囊，用于导出、查看、复核等辅助动作。
- `panel` / `metric-card`：白色半透明或白色卡片，12px 圆角、浅边框、轻阴影。
- `console-card`：深色数据控制台，用于 Hero 预览、数据统计、训练日志等高价值模块。
- `status-pill`：状态用胶囊标签，颜色同时编码语义，文本短而明确。
- `form-input`：白底、8px 圆角、冷色边框，focus 使用青蓝光环。
- `DataTable`：浅色表头、稳定行高、横向滚动、hover 行高亮，数字列使用 tabular figures。
- 图标统一使用 `lucide-react`，按钮和数据模块优先使用图标+短文本。

## 状态与交互

- 保留 loading、empty、error、disabled、selected、focused、success 状态。
- 卡片 hover 可轻微上浮，按钮 hover 只增强阴影和背景，不改变布局。
- 上传区 hover 强化边框和背景，识别处理中显示进度条。
- 导航选中使用胶囊背景和细微高亮；移动端保留横向滚动导航。
- 尊重 `prefers-reduced-motion`，关闭非必要动画。

## 可访问性与响应式

- 所有导航、按钮、表单保持语义化和可见焦点。
- 图标按钮必须有可访问名称或配套文本。
- 表格窄屏横向滚动，长文本换行或截断，不挤压操作按钮。
- 移动端保持品牌、主操作、状态和核心数据可见。

## 工程约束

- 优先修改 `frontend/src`，不改后端识别逻辑。
- 不新增大型依赖；继续使用 React、Vite、Tailwind、lucide-react 和现有组件。
- 样式集中在 `frontend/src/styles.css`；页面层只补充必要结构和业务内容。
- 保留现有 API 调用：`/api/detect`、`/api/batch`、`/api/history`、`/api/stats`、`/api/distribution/*`、报告导出等。

## 移动端部署约束

- Vite 生产构建使用根路径 `base: '/'`，静态图像继续从 `src/assets` import，由 Vite 打包为 hashed assets；禁止使用本机绝对路径或 `/frontend/` 路径。
- 移动端最终样式层放在 `frontend/src/styles.css` 末尾，负责 375px、390px、430px 下的防横向溢出、单列布局、表格横向滚动、移动端导航和弹窗高度约束。
- API 地址只从 `VITE_API_BASE_URL` 读取；未配置公网后端时，页面进入离线服务状态并在上传、识别、AI 分析、报告导出等动作中给出友好提示。
- Vercel 使用 `frontend/vercel.json` 将所有前端路由 rewrite 到 `index.html`，保证二级页面刷新不会 404。
